
For "config" directory:

try to make run: mplayer -ao alsa:device=hw=1.1 -srate 32000 **/*mp3
with snd-aloop command: sudo modprobe snd-aloop

next, sound will be encoded like this: dabplus-enc -d hw:1 ... in enc-radio.conf file


